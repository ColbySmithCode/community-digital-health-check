{
  "name": "digital-health-check-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for the Digital Health Check — free digital audits for nonprofits and small businesses",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
